{
  "name": "odoo-json2",
  "version": "0.1.1",
  "description": "MCP tools for Odoo 19 External JSON-2 (POST /json/2/{model}/{method}).",
  "author": {
    "name": "Tyler Harden",
    "email": "root@tylerjharden.dev"
  },
  "homepage": "https://tylerjharden.dev",
  "repository": "https://github.com/tylerjharden/odoo-json2",
  "license": "MIT",
  "keywords": ["odoo", "json-2", "erp", "mcp"],
  "logo": "assets/logo.svg",
  "variables": {
    "type": "object",
    "properties": {
      "ODOO_URL": {
        "type": "string",
        "title": "Odoo URL",
        "description": "Host or origin, e.g. mycompany.odoo.com or https://mycompany.odoo.com. https:// is added if omitted. No path."
      },
      "ODOO_API_KEY": {
        "type": "string",
        "title": "Odoo API key",
        "description": "User API key from Preferences → Account Security → New API Key."
      },
      "ODOO_DATABASE": {
        "type": "string",
        "title": "Odoo database",
        "description": "Database name. Sent as X-Odoo-Database on every JSON-2 call (required)."
      }
    },
    "required": ["ODOO_URL", "ODOO_API_KEY", "ODOO_DATABASE"]
  }
}
